import Task from './Task';
import Board from './Board';

let board = new Board();

board.addTask( new Task('Doctor', 'After braking the tooth have to check it', new Date()) );
board.addTask( new Task('Doctor', 'After braking the tooth have to check it', new Date()) );
board.addTask( new Task('Doctor', 'After braking the tooth have to check it', new Date()) );
board.addTask( new Task('Doctor', 'After braking the tooth have to check it', new Date()) );
board.addTask( new Task('Doctor', 'After braking the tooth have to check it', new Date()) );
board.addTask( new Task('Doctor', 'After braking the tooth have to check it', new Date()) );